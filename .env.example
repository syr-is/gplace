DB_PASSWORD=dogwater
REDIS_PASSWORD=password
# Host port mappings (defaults are offset to coexist with syr (5173/5432/6379-ish), syren (5174-5175), etc.)
DEV_PORT=5176
STUDIO_PORT=5556
DB_PORT=5476
REDIS_PORT=6479
APP_PORT=3076
APP_ORIGIN=http://localhost:5176
PUBLIC_CURRENT_BOARD=main
PROD=false
REDIS_USER=default
# Connection strings — placeholder values that satisfy build-time tooling.
# Real values are injected at runtime (dev compose constructs them from the parts above; prod gets them from Dokploy managed services).
DATABASE_URL=postgresql://postgres:password@localhost:5432/postgres
REDIS_URL=redis://localhost:6379
